import { MessageScreen } from '../messageScreen';

const SuccessLoginScreen = () => (
  <MessageScreen
    title="User Successfully logged in"
    timer={500}
    callback={() => {}}
  />
);

export { SuccessLoginScreen };
